import {AppRegistry} from 'react-native';
import Navigator from './src/components/Telas/Navigator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navigator);
